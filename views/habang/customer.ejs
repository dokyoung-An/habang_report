<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link href="/css/main.css" rel="stylesheet" />
    <link href="/css/inspect-registorform.css" rel="stylesheet" />
    <title>고객정보등록</title>
  </head>
  <body>
    <div class="container">
     

      <!-- 상단 네비게이션 -->
  
      <ul class="nav nav-tabs">
        <a class="nav-link logo-link" href="/">
            <img src="/image/logo_bl.png" alt="로고" class="logo-image">
        </a>
        <li class="nav-item">
            <a class="nav-link active" href="/">
                <i class="bi bi-house-door"></i> 고객정보등록
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="/customer-list">
                <i class="bi bi-list-check"></i> 보고서리스트
            </a>
        </li>
    </ul>
    </ul>
       <!-- 페이지 타이틀 -->
       <h1 class="page-title">
        <i class="bi bi-pencil-square"></i>
        고객의 정보를 입력하세요
    </h1>

      <!-- 메인 폼 -->
      <div class="form-container">
          <form action="/register" method="POST">
              <!-- 기본 정보 섹션 -->
              <div class="form-section">
                  <div class="row g-4">
                      <div class="col-md-6">
                          <label class="form-label">
                              <i class="bi bi-calendar"></i> 점검 날짜
                          </label>
                          <input class="form-control" type="date" required name="date">
                      </div>
                      <div class="col-md-6">
                          <label class="form-label">
                              <i class="bi bi-building"></i> 아파트명
                          </label>
                          <input class="form-control" type="text" required name="apart" placeholder="아파트 이름을 입력하세요">
                      </div>
                  </div>
              </div>

              <!-- 위치 정보 섹션 -->
              <div class="form-section">
                  <div class="row g-4">
                      <div class="col-md-6">
                          <label class="form-label">
                              <i class="bi bi-house"></i> 동
                          </label>
                          <input class="form-control" type="text" required name="dong" placeholder="동 번호를 입력하세요" oninput="this.value = this.value.replace(/[^0-9]/g, '')">
                      </div>
                      <div class="col-md-6">
                          <label class="form-label">
                              <i class="bi bi-door-closed"></i> 호수
                          </label>
                          <input class="form-control" type="text" required name="home" placeholder="호수를 입력하세요" oninput="this.value = this.value.replace(/[^0-9]/g, '')">
                      </div>
                  </div>
              </div>

              <!-- 고객 정보 섹션 -->
              <div class="form-section">
                  <div class="row g-4">
                      <div class="col-md-6">
                          <label class="form-label">
                              <i class="bi bi-person"></i> 고객명
                          </label>
                          <input class="form-control" type="text" required name="name" placeholder="고객명을 입력하세요">
                      </div>
                      <div class="col-md-6">
                          <label class="form-label">
                              <i class="bi bi-telephone"></i> 전화번호
                          </label>
                          <input class="form-control" type="tel" required name="phone" placeholder="전화번호를 입력하세요" id="phone" maxlength="13">
                      </div>
                  </div>
              </div>

              <!-- 제출 버튼 -->
              <div class="text-center">
                  <button type="submit" class="btn btn-primary submit-button">
                      <i class="bi bi-check-lg"></i> 고객 정보 저장
                  </button>
              </div>
          </form>
      </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    document.addEventListener("DOMContentLoaded", function() {
    const phoneInput = document.getElementById("phone");

    phoneInput.addEventListener("input", function(e) {
        let value = e.target.value.replace(/[^0-9]/g, ""); // 숫자만 허용
        if (value.length > 3 && value.length <= 7) {
            value = value.replace(/(\d{3})(\d+)/, "$1-$2");
        } else if (value.length > 7) {
            value = value.replace(/(\d{3})(\d{4})(\d+)/, "$1-$2-$3");
        }
        e.target.value = value;
    });
});

  </script>
  </body>
</html>
